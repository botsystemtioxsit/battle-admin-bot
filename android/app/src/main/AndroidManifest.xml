<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- НЕ добавлять REQUEST_INSTALL_PACKAGES обратно: с ним приложение
         само скачивало и ставило APK поверх себя (см. MainActivity —
         checkForUpdate), а Google Play Защита распознаёт именно эту
         связку (сторонний неизвестный APK + право ставить другие APK) как
         поведение трояна-дроппера и блокирует установку самого
         приложения целиком. Обход через Google Play Console/Internal App
         Sharing стоит денег — этот проект принципиально ничего платного
         не использует — поэтому checkForUpdate теперь только открывает
         ссылку на APK в браузере, разрешение на установку пакетов
         приложению не нужно вообще. -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
